# 「먹고 마시는 방」(먹고 마시기) — 방 설계서

> `docs/puzzles/room1-overview.md`(1번 방)의 절 구성을 따른다.

방 = `food-room` — 급식실/부엌 한쪽의 식사 공간. 배경 아트는 아직 없다(뼈대 단계,
1번 방과 동일한 사정 — 이미지 생성 도구가 이 세션에 없음). 크기는 임시 14×14.
6방 계획의 **2번 방**. 「딱! 이만큼」 앱의 food 카테고리(FO-001~020) 20문항 중
4문항을 퍼즐로, 4문항(별도)을 최종 서열 퍼즐로 재사용한다.

**방 방향**: 1번 방과 같은 접근성 원칙(이산 탭만, 반응속도·소근육 정밀도가 판단을
가리지 않게). 1번 방에 없던 조작 두 가지를 새로 도입한다 — **넓이-격자 탭**(펴 바르기)과
**관찰 후 단발 입력, 부족해지기 전에 멈추기**(brushing-timer의 반대 방향).

## 방 구조

```
입장 (spawn) ─ P1 물 따르기 / P2 얼음 넣기 / P3 잼 바르기 / P4 요거트 토핑
              (네 곳 모두 봉인 밖 — 순서 자유)
식탁 정리대 코너 [sealed shelf-corner, opensWhen: 4개 code:*-solved 전부]
  ─ 최종 콘솔 pour-shelf-order(서열 퍼즐, gate 없음) → door:food-open → 출구
```

1번 방과 같은 뼈대(4개 자유 순서 퍼즐 + 봉인된 코너 + 서열 콘솔)를 그대로 쓴다 —
방마다 새 구조를 발명하지 않는 것이 학생에게도, 유지보수에도 유리하다.

## 정보 배정표 (코드 조각 대신 — 각 퍼즐이 공개하는 정보)

| 퍼즐 | 근거 문항 | 조작 | 판정 핵심 | 보상(이벤트) |
|---|---|---|---|---|
| P1 water-pour | FO-001 | **탭으로 따르기**(누를 때마다 물 높이 증가, "그만 따르기" 확정) | 손가락 한 마디 구간 | `code:water-solved` |
| P2 ice-drop | FO-008 | **탭으로 넣기 횟수 세기**(되돌리기 없음) | 2~5개 | `code:ice-solved` |
| P3 jam-spread | FO-005 | **격자 칸 탭**(칸을 눌러 펴 바르고, 다시 눌러 되돌림) | 넓이 퍼센트 구간 | `code:jam-solved` |
| P4 yogurt-topping | FO-017 | **관찰 후 단발 탭**(토핑이 자동으로 하나씩 떨어짐, 사라지기 전에 "그만!") | 흰 부분이 남아있는 구간 | `code:topping-solved` |
| 최종 pour-shelf-order | FO-018·002·001·013 | **탭으로 순서 배치**(적은 것부터) | 정답 순열 유일 | `door:food-open` |

조작 4종 중복 없음: 채우기(누적 후 확정, 1번 방 toothpaste와 같은 계열이지만 이 방
안에서는 유일) / 횟수 세기(1탭=1개, 되돌리기 없음) / **넓이 격자 채우기**(1번 방에
없던 신규 조작) / **관찰 후 단발 입력**(brushing-timer와 짝이지만 "멈추는 방향"이
반대 — 사라지기 전에 눌러야 한다).

---

## P1 `water-pour` — 물 따르기 (FO-001) · 하

- 컵 그림. [따르기] 버튼을 탭할 때마다 물 높이가 한 단계씩 오른다(이산 스텝).
  [그만 따르기]로 확정, [덜어내기]로 확정 전 한 단계 되돌리기 가능.
- 정답 구간: 컵 맨 위에서 손가락 한 마디 아래(원본 answer 55~78 그대로, 정규화 스텝).
- 보상: `code:water-solved`.

## P2 `ice-drop` — 얼음 넣기 (FO-008) · 하

- 컵에 얼음을 하나씩 넣는 그림. [넣기] 탭마다 얼음이 1개씩 쌓인다. 되돌리기 없음
  (한 번 넣은 얼음은 꺼내지 않는다는 자연스러운 제약 — hand-sanitizer-pump와 같은
  설계). 오답(too high) 확정 시 컵을 비우고 재도전.
- 정답: 2~5개(원본 그대로).
- 보상: `code:ice-solved`.

## P3 `jam-spread` — 잼 바르기 (FO-005) · 중

- 식빵을 6×4(또는 축소한) 격자로 나눠, 칸을 탭하면 그 칸에 잼이 발린다(다시 탭하면
  지워짐 — 무오류 학습, 확정 전 자유 수정). [됐어요]로 확정하면 칠해진 칸 비율(%)을
  정답 구간(45~72%, 원본 areaPercent 그대로)과 비교.
- imankeum의 `SpreadArea`/`CoverageScene` 발상을 이산 탭으로 옮긴 것 — 드래그 없이
  같은 "면적 판단" 개념을 다룬다.
- 보상: `code:jam-solved`.

## P4 `yogurt-topping` — 요거트 토핑 (FO-017) · 중

- 요거트 그릇 그림. 토핑이 TICK_MS 간격으로 **자동으로 하나씩 떨어지는** 애니메이션이
  진행된다(사용자 입력 없이 진행 — brushing-timer와 반대로 "부위가 다 밝혀지는" 것이
  아니라 "흰 부분이 점점 줄어드는" 장면). 흰 부분이 아직 보이는 동안 [그만!]을 누르면
  정답, 다 덮인 뒤 누르면 too high, 첫 토핑도 떨어지기 전에 누르면 too low.
- 반응속도가 아니라 "흰 부분이 아직 보이는가"라는 관찰 판단만 남긴다(brushing-timer와
  같은 접근성 원칙, curriculum-map.md 예외 표 참조).
- 보상: `code:topping-solved`.

## 최종 `pour-shelf-order` — 식탁 정리대 서열 퍼즐 (FO-018·002·001·013) · 중상

- 4개 퍼즐을 모두 풀면 정리대 봉인이 걷히고 이 장치가 열린다.
- 화면에 용기 4개(나눠 마시는 주스잔·시리얼 우유·물컵·물병) 카드가 뒤섞여 표시된다.
  **적게 따르는 것부터 많이 따르는 순서로** 하나씩 탭해 선반 4칸에 놓는다(되돌리기
  1단계 지원).
- 정답 순서(각 문항의 "알맞은 양" 구간 중앙값 오름차순, `autoplay.ts`가 유일 정답임을
  순열 전수로 검산): 나눠 마시는 주스잔 FO-018(33) → 시리얼 우유 FO-002(52.5) →
  물컵 FO-001(66.5) → 물병 FO-013(83.5).
- 다 놓으면 즉시 채점(정답이면 `door:food-open` 발화, 오답이면 방향 피드백 후 선반을
  비우고 재시도).
- 힌트 단계: ① "여럿이 나눠 마실 때랑 혼자 물병에 채울 때, 어느 쪽에 더 조심할까?"
  ② "제일 적게 따르는 건 셋이 나눠 마시는 주스잔이야." ③ 정답 순서 중 인접 두 개를
  직접 알려줌.
